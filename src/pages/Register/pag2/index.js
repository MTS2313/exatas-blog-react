const Pag2 = ({updateIndex}) => {

    return (<>
                2
                    <button onClick={() => updateIndex(1)}>back</button>
                    <button onClick={() => updateIndex(3)}>next</button>
    </>)
}

export default Pag2;